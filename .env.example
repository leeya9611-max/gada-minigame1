# Supabase (E7-1) — 서버 전용. NEXT_PUBLIC_ 접두사 절대 금지(service_role 키 유출 = DB 전체 개방).
# 값은 Supabase 대시보드 → Project Settings → API 에서 복사. 로컬은 .env.local, 배포는 Vercel 환경변수에.
# 두 변수 미설정 시 API는 메모리 스텁으로 폴백(로컬 개발·헤드리스 검증용, 재시작 시 초기화).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
