# 개발 작업지시서 v3 — 한 달 챌린지 이벤트 피벗 (클로드코드용)

기준: `docs/야리끼리_한달챌린지_피벗기획_v1.md` · 기존 코드(작업지시서 v2의 WP0~6.5 완료 상태) · 2026-07
사용법: **E1부터 순서대로** 각 단계의 "클로드코드 프롬프트" 블록을 복사해 클로드코드에 입력. 한 단계 완료·확인 후 다음으로.

## 피벗 요약 (v2 대비 무엇이 바뀌나)

- 랭킹 축: 노선 클리어 타임 → **엔들리스 "무한 잔업 모드" 단일 지표**(일일 베스트 합산 + 주간 리셋 4라운드)
- 로비: 월드맵 5노선 선택 → **버튼 2개 [안전교육] [무한 잔업 모드]**
- 안전교육 = 90초 한 판 튜토리얼(티켓 미차감, 구간별 기술 학습, 이수 시 본선 해금)
- 컷: 월드맵 UI, 별점·해금 체계 노출, 캐릭터 상점(WP7 취소). route2~5 데이터는 삭제하지 않고 보관
- 이벤트 장치: 라운드 D-day, 주차 테마 팔레트 스왑

## 현재 구현 상태 (재작업 금지)

v2의 WP0~6.5 완료: 가로 화면, 피격 룰, 박소장 추격(gap), 슬라이드, 스프라이트, 티켓·결과 postMessage, 노선(JSON) 로더·완주 판정·피날레, 로비(월드맵)·별점·해금.

이번에 추가 반영된 것:
- **닉네임 등록**(S1.2): 자동 생성(현장 감성, 주사위 리롤) + 직접 입력 + 중복 확인. `lib/nickname.ts`, `app/api/nickname/route.ts`(개발 스텁 — 운영은 서버 교체), 결과값에 `nickname` 포함
- **양손 조작**: 좌 절반 홀드=슬라이드 / 우 절반 탭=점프, pointerId 멀티터치 추적, 인게임 조작 힌트 4초

| 단계 | 제목 | 핵심 | 선행 |
|---|---|---|---|
| **E1** | 엔들리스 "무한 잔업 모드" | 절차 스폰 복원, mode 구분, 결과값 확장 | - |
| **E2** | 로비 2버튼 개편 + 안전교육 게이트 | 월드맵 제거, 이수 플래그, 티켓 정책 분리 | E1 |
| **E3** | 안전교육 노선(구간 학습) | route_edu 데이터, 구간 배너, 무한 관용 | E2 |
| **E4** | 주간 시즌·일일 베스트 랭킹 | 서버 계약+스텁, 랭킹 화면 개편, D-day | E1 |
| **E5** | 주차 테마 팔레트 스왑 | 배경 필터로 주차별 분위기 변주 | E1 |
| **E6** | QA·밸런싱·폴리시 | 웹뷰 제스처, 엔들리스 밸런스, 로깅 | 전체 |

---

## E1. 엔들리스 "무한 잔업 모드" (랭킹 본선)

목표: WP6에서 노선(레벨) 재생으로 바뀐 엔진에 **레벨 없이 무한히 달리는 모드**를 되살린다. 이벤트 랭킹의 유일한 점수 소스.
대상: `GameEngine.ts`(mode 상태·절차 스폰 경로), `config.ts`(엔들리스 밸런스), `types.ts`/`lib/api.ts`(결과값 mode 필드).
완료 기준(AC):
- `engine.setEndless()`(또는 동급 API)로 시작하면 레벨 데이터 없이 절차 스폰(장애물·코인·아이템·투척)으로 무한 진행.
- 완주 판정·피날레 돌진 없음. 속도는 BASE에서 지속 램프(MAX 상한), 박소장 추격(gap)·HP 실패 조건은 동일.
- 점수 산식 유지(코인×10+거리÷10). 결과값에 `mode: "endless" | "edu" | "route"` 추가, 엔들리스는 `routeId: "endless"`.
- 기존 노선 재생 경로는 그대로 동작(안전교육이 사용).

```
[클로드코드 프롬프트 — E1]
docs/야리끼리_한달챌린지_피벗기획_v1.md 2장대로 엔들리스 모드를 복원해줘.
1) GameEngine에 mode("route" | "endless") 상태를 추가하고, setEndless() 시작 API를 만들어.
   엔들리스는 레벨 데이터 없이 예전 절차 스폰(SPAWN 상수 기반 장애물/코인/아이템/투척 랜덤 스폰)으로 무한 진행.
   완주 판정과 ROUTE.FINALE 돌진은 엔들리스에서 비활성. 속도는 SPEED.BASE에서 ACCEL로 램프(MAX 상한).
   박소장 추격(gap)·HP 실패 조건, 점수 산식은 그대로.
2) 결과값(GameResult)에 mode 필드를 추가하고 엔들리스는 routeId "endless"로 전달. lib/api.ts 타입도 갱신.
3) 기존 노선 재생(setLevel) 경로는 건드리지 말 것(안전교육에서 계속 사용).
4) 임시 확인용으로 로비에서 엔들리스를 시작할 수 있는 개발용 진입을 하나 만들어(다음 단계 E2에서 정식 버튼으로 교체).
npx tsc --noEmit 통과 확인하고 실제 플레이(무한 진행·게임오버·결과 전달)로 검증한 뒤 변경 요약 보여줘.
```

---

## E2. 로비 2버튼 개편 + 안전교육 게이트

목표: 월드맵(노선 5개 선택)을 걷어내고 로비를 [안전교육] [무한 잔업 모드] 2버튼으로. 안전교육 1회 이수가 본선 해금 조건.
대상: `Game.tsx`(LobbyScreen 교체), `lib/progress.ts`(이수 플래그), 티켓 정책 분기.
완료 기준(AC):
- 로비: 닉네임·티켓 잔량 + 큰 버튼 2개. 무한 잔업 모드는 안전교육 미이수 시 잠금 표시("안전교육 이수 후 참가 가능").
- 안전교육: **티켓 미차감**, 언제든 재입장 가능. 완주 시 이수 플래그 저장(우선 localStorage, 서버 이관 TODO 주석) → 본선 즉시 해금 + "무한 잔업 모드 개방!" 안내.
- 무한 잔업 모드: 티켓 1장 차감(기존 consumeTicket), 0이면 충전 화면(S5) 기존 흐름.
- 별점·해금·월드맵 UI 제거(관련 함수·데이터는 삭제하지 말고 미사용 보관). 상점 버튼 제거(WP7 취소).
- 완주 성공 화면(S3a)은 안전교육 전용이 되고, 버튼은 [무한 잔업 모드 가기]/[다시 연습]으로.

```
[클로드코드 프롬프트 — E2]
docs/야리끼리_한달챌린지_피벗기획_v1.md 2장·2.5대로 로비를 개편해줘.
1) LobbyScreen을 월드맵에서 2버튼 구성으로 교체: [안전교육](부제 "조작 연습 · 무료")과
   [무한 잔업 모드](부제 "랭킹전 · 티켓 1장"). 헤더의 닉네임·티켓 표시는 유지, 상점 버튼은 제거.
2) lib/progress.ts에 안전교육 이수 플래그(loadEduDone/saveEduDone, localStorage, 서버 이관 TODO 주석)를 추가해.
3) 안전교육 시작은 티켓을 차감하지 않게 startRoute와 분리하고, route1 레벨로 재생(E3에서 route_edu로 교체 예정).
   완주(cleared) 시 이수 플래그 저장 + 완주 화면에 "무한 잔업 모드 개방!" 문구와 [무한 잔업 모드 가기] 버튼.
4) 무한 잔업 모드 버튼은 이수 전 잠금(회색 + "안전교육 이수 후 참가 가능"), 이수 후 티켓 차감하고 engine.setEndless()로 시작.
5) 별점·월드맵 관련 UI 코드는 렌더에서만 떼고 함수·route2~5 데이터는 보관. E1의 개발용 진입은 제거.
npx tsc --noEmit 통과, 신규 유저 흐름(교육→해금→본선)과 재방문 흐름(로비 직행)을 확인하고 요약 보여줘.
```

---

## E3. 안전교육 노선 — 한 판 안 구간 학습

목표: 안전교육 전용 레벨 `route_edu`를 만들고, 구간별 기술 학습(점프→이단점프→슬라이드→투척→미니 추격→정류장)과 안내·관용 룰을 넣는다.
대상: `public/levels/route_edu.json`(신규), `level.ts`(안내 마커 확장), `GameEngine.ts`(교육 모드 룰), `Game.tsx`(구간 배너).
완료 기준(AC):
- route_edu: 90초 내외, 구간 1 웅덩이(점프)만 → 2 자재더미·연속(이단점프) → 3 lowbar(슬라이드) → 4 투척 '!'(상·하) → 5 짧은 추격 압박 후 정류장. 초반 속도 낮게(baseSpeed 260 내외), 램프 완만.
- 레벨 데이터에 구간 안내 마커(예: `markers: [{col, text}]`)를 추가하고 해당 col 통과 시 상단 배너로 2초 표시("탭해서 점프!" 등, 기획 2.1 표 문구).
- 신규 요소 첫 등장 col 직전에 일시 감속(예: 0.7배 2초) 1회씩.
- 교육 모드 관용: 피격 시 HP 미감소(넉백·감속 연출은 유지) + gap 실패 없음(잡히기 직전까지만 압박). 즉 실패 없이 완주 유도.
- 투척은 구간 4부터만 발생(그 전 GRACE).

```
[클로드코드 프롬프트 — E3]
docs/야리끼리_한달챌린지_피벗기획_v1.md 2.1대로 안전교육 노선을 구현해줘.
1) public/levels/route_edu.json 생성(map-editor 포맷). 90초 내외, baseSpeed 260, 구간 구성:
   1 웅덩이 점프만 → 2 자재더미·연속 장애물(이단점프) → 3 lowbar 슬라이드 → 4 투척 상·하 회피 → 5 미니 추격 → 정류장.
   코인은 각 구간 동선을 따라 배치해 조작을 유도해.
2) LevelData에 markers?: {col:number; text:string}[] 를 추가하고, 통과 시 상단 배너로 2초 표시:
   "탭해서 점프!"/"공중에서 한 번 더!"/"아래로 밀어(왼쪽 꾹) 슬라이드!"/"!가 뜨면 피하세요"/"정류장이 보인다! 달려!"
3) 신규 요소 첫 등장 직전 1회 일시 감속(0.7배 2초).
4) 교육 모드(edu) 룰: 피격 시 HP 미감소(피격 연출·감속은 유지), gap<=0 실패 비활성(최소 gap에서 클램프),
   투척은 구간 4부터. E2의 안전교육 시작을 route_edu로 교체.
npx tsc --noEmit 통과, 처음 하는 사람 기준으로 실패 없이 완주되는지 플레이 확인 후 요약 보여줘.
```

---

## E3.5. 폴리시 패스 — 플레이 영상 리뷰 반영 (2026-07-13)

목표: E3까지의 테스트 영상에서 확인된 어색한 부분 수정. E4 진행 전 처리.
근거: 화면 기록 리뷰. 항목별 원인 분석 포함.

| # | 증상 | 원인/방향 |
|---|---|---|
| 1 | 박소장이 gap과 무관하게 항상 김반장에 밀착 | PLAYER.X(168) < gap 범위(170~210) → 렌더 x가 항상 음수라 가장자리 클램프. 김반장 x 이동 + gap 범위 재조정 |
| 2 | 배경 실루엣 레이어가 화면 중간에서 끊김·이음새 | 패럴럭스 타일링 버그(반복 채움 누락·레이어 높이 불일치). 지면 색 밴드 이음새 포함 |
| 3 | 위기 연출이 김반장 뒤 "붉은 원" | 과녁처럼 보임 → 화면 가장자리 비네트로 교체 |
| 4 | 교육 90초 내내 위험!·잔소리 → 혼나는 경험 | edu 모드 연출 완화(위기 연출 억제·대사 빈도 축소), 클리어 화면 "피격 N회" → 긍정 프레임 |
| 5 | 슬라이드 모션이 넘어짐처럼 보임 / 투척물이 박소장 얼굴 가림 / 흰 서류 투척물 가시성 낮음 / '!' 경고 작음 / 박소장 공중에 뜸 / 하트 아이템 톤 이질 | 스프라이트 렌더·스폰 좌표·가시성 개별 수정 |
| 6 | 진행도 바가 빨갛게 차오름 | 진행도=위험으로 오독. 진행 fill 색 고정, 피날레 연출 분리 |
| 7 | 정류장 뒤 지면이 검은 절벽 | 레벨 끝 이후 지형 렌더 미처리 |
| 8 | 좌상단 추격 게이지 의미 불명 | 극성 안 읽힘 + 박소장 밀착 버그로 그림과 따로 놈 → **게이지 삭제**, 박소장 거리+비네트가 위험 신호 역할 |
| 9 | 안전교육 91초 — 길고 지루 | 빈 직선 구간 다수(밀도 문제 겸). 60초 전후 압축 + 구간 밀도 재편 |
| 10 | 투척 직후 즉시 명중 — 반응 불가 | 박소장이 화면 안으로 들어오며 비행 거리 ~200px·0.4초로 축소. 경고 선행+상대 속도 하향으로 반응 창 보장 |
| 11 | lowbar를 점프로 넘을 수 있음 | 상단 히트박스 미차단 → 슬라이드 무용지물, 2지선다 붕괴. 상단 골조로 막기 |

```
[클로드코드 프롬프트 — E3.5]
테스트 영상 리뷰에서 나온 폴리시 이슈를 수정해줘. 우선순위 순서대로.
1) 추격 시각화 수리(최우선): 현재 PLAYER.X(168)가 CHASE gap 범위(170~210)보다 작아 박소장 렌더 x(PLAYER.X - gap)가
   항상 음수 → 가장자리 클램프 → gap이 변해도 화면상 박소장이 늘 김반장에 붙어 보인다.
   PLAYER.X를 300 내외로 옮기고(스폰·판정 파생값 확인), CHASE를 START_GAP 200 / MAX_GAP 280 수준으로 재조정해
   안전할 때 박소장이 화면 왼쪽에 멀찍이, gap이 줄수록 실제로 다가오는 게 보이게 해줘. 시야가 준 만큼 반응 여유는
   투척 WARNING_MS·장애물 스폰 간격으로 보정.
2) 배경 패럴럭스 타일링: 실루엣 레이어가 화면 중간에서 끊기고 오른쪽이 빈 하늘이 되는 버그를 고쳐줘
   (레이어 폭만큼 반복 드로우, 스크롤 오프셋 모듈로 처리). 지면 텍스처의 색 밴드 이음새도 같이.
3) 위기 연출: 김반장 뒤 붉은 원을 제거하고 화면 가장자리 붉은 비네트(gap 비율에 따라 알파)로 교체.
4) 안전교육 톤: edu 모드에서는 위기 비네트·"위험!" 표시를 끄고 박소장 잔소리 대사 빈도를 절반 이하로.
   클리어 화면의 "피격 N회"를 빼고 "점프·슬라이드·회피 3가지 조작 이수" 형태의 긍정 문구로.
5) 스프라이트·가시성 일괄:
   - 슬라이드 렌더가 넘어짐(hurt)처럼 보이지 않게 slide 프레임 사용·각도 확인, 먼지 이펙트로 "미끄러짐" 명시.
   - 투척물 스폰 위치를 박소장 손 앞(우측 상단 오프셋)으로 — 현재 얼굴에 겹침.
   - 서류(papers) 투척물에 외곽선·회전을 주고 크기를 키워 하늘 배경에서 보이게.
   - 투척 경고 '!'를 크게, 등장 시 펄스 애니메이션.
   - 박소장 y를 지면에 고정(공중에 뜨는 프레임 제거), 장애물과 겹칠 때는 뒤 레이어로.
   - 하트 아이템을 다방커피/부스터와 같은 톤의 스프라이트로(임시면 코인 스타일 원형 배지로 통일).
6) 진행도 바: fill 색을 노랑 고정(빨강 금지). 피날레 구간은 바 색이 아니라 정류장 아이콘 깜빡임과
   박소장 돌진 연출로 표현.
7) 노선 끝(정류장 이후) 지형: 레벨 마지막 col 이후에도 지면을 이어 그려 검은 절벽이 보이지 않게.
8) HUD 단순화: 좌상단 박소장 추격 게이지를 삭제해줘(관련 코드는 미사용 보관). 위험 신호는 1번(박소장
   실거리)과 3번(가장자리 비네트)이 담당한다. 남는 HUD는 안전모(HP)·코인/점수·정류장 진행도 3개.
9) 안전교육 압축: route_edu.json을 60초 전후로 재편집해줘. 원칙 —
   - 빈 직선 구간 제거. 구조는 "구간 배너 → 해당 조작 실습 2~3회 → 즉시 다음 구간".
   - 각 구간 8~12초: 1 점프 → 2 이단점프 → 3 슬라이드 → 4 투척 회피(상·하 1회씩) → 5 피날레 5초+정류장.
   - 코인을 조작 동선(점프 궤적·슬라이드 라인)에 깔아 유도. baseSpeed를 구간마다 소폭 상승시켜
     교육 종반에 본게임 템포를 예고.
10) 투척 반응 창 보장: 박소장이 화면 안에 보이게 되면서 투척물 비행 거리가 gap(~200px)으로 줄어
    던지자마자 맞는다(470px/s 기준 0.4초 — 반응 불가).
    - '!' 경고를 투척 모션보다 WARNING_MS(1.6초) 선행시키고, 경고에 상단/하단 위치를 표시.
    - 투척물의 플레이어 기준 상대 접근 속도를 낮춰(예: 250~300px/s) 투척 후 명중까지 0.7초 이상 확보.
    - "경고 시작 → 명중"까지 최소 2초를 상수로 보장하는 구조로(스폰 시점 역산).
11) lowbar 상단 차단: 현재 점프로 넘을 수 있어 슬라이드가 무용지물(2지선다 붕괴).
    - 히트박스를 바 높이부터 화면 상단 방향으로 채워 1·2단 점프 모두 충돌하게.
    - 렌더도 위로 골조가 이어진 비계/파이프 구조물로 바꿔 "밑으로 지나가야 함"이 한눈에 읽히게.
    - 슬라이드 통과 판정(히트박스 48px 이하)은 기존 유지.
각 항목 수정 후 npx tsc --noEmit 통과 확인, 안전교육 1판 완주 영상 기준으로 1·2·3·8·9번이 눈에 띄게
개선됐는지 확인하고 변경 요약을 보여줘. 10번은 투척 5회 중 4회 이상 회피 가능한지, 11번은 lowbar 앞
이단점프 시 충돌하는지로 검증.
```

---

## E3.6. 게임필 패스 — 점프 물리 · 잡힘 연출 · 주스 (2026-07-15 영상 리뷰)

목표: 레퍼런스(JOJO vs ZOMBIE류 캐주얼 러너) 대비 부족한 손맛·연출 보강. 아트 에셋 교체(배경 레이어·장애물 스프라이트)는 별도 트랙(AI 생성/외주)으로 진행하고, 여기서는 코드로 되는 것만.

| # | 증상 | 방향 |
|---|---|---|
| 1 | 2단 점프 최고점이 화면 밖(머리가 HUD 뚫음) | 물리 재조정: 총 점프 높이 ≈ 지면 위 200~220px 상한 |
| 2 | 잡힘 룰이 체감 안 됨(교육에선 무반응) | 교육=헛손질 예고 연출, 엔들리스=명확한 잡힘 연출 |
| 3 | 밋밋한 게임필 | 주스: 착지 먼지, 코인 회전·반짝, 피격 셰이크, 낙하 가속 |

```
[클로드코드 프롬프트 — E3.6]
게임필 패스를 진행해줘.
1) 점프 물리 재조정: 현재 2단 점프 최고점이 화면을 벗어난다. 목표 — 1단 점프 지면 위 ~130px,
   2단 누적 최고점 ~200-220px(캐릭터 머리가 항상 HUD 아래). DOUBLE_JUMP_V를 낮추고(520 내외에서 튜닝),
   하강 시 중력을 상승 시의 1.3배로 적용해(낙하 가속) 붕 뜨는 느낌을 없애줘. 모든 장애물·상단 투척이
   조정된 점프 높이로 회피 가능한지 확인하고 필요한 높이값을 함께 조정해.
2) 잡힘 연출:
   - 엔들리스: gap<=0 시 즉시 오버가 아니라 0.6초 잡힘 연출(박소장이 김반장 멱살 잡는 포즈로 겹침,
     화면 정지+줌 느낌, "붙잡혔다!" 텍스트) 후 게임오버 화면. 기존 실패 판정 로직은 유지.
   - 안전교육: gap이 최소치 도달 시 1회성 이벤트 — 박소장 헛손질 모션(팔 뻗기) + 대사
     "박소장: 교육 중이니 봐준다... 본게임엔 어림없어!" + gap 소폭 회복. 이후 반복 시 연출 생략.
3) 주스(모두 캔버스 이펙트, 에셋 추가 없이):
   - 착지 시 먼지 퍼프 2~3개, 달리는 동안 발밑 미세 먼지.
   - 코인: 세로축 회전(가로 스케일 진동) + 간헐 반짝 스파클, 획득 시 위로 튀며 페이드+점수 플로터.
   - 피격: 화면 셰이크 4px·0.2초 + 히트스톱 0.05초.
   - 부스터 중 잔상(모션 트레일).
4) 슬라이드 먼지 이펙트가 없다면 슬라이드 중 지면 마찰 먼지도 추가.
npx tsc --noEmit 통과, 플레이 확인(점프 최고점이 HUD 아래인지, 교육·엔들리스 잡힘 연출 각 1회)
후 변경 요약 보여줘.
```

### E3.7. 에셋 반입 후 시각 품질 패스 (2026-07-16 영상 리뷰)

| # | 증상 | 방향 |
|---|---|---|
| 1 | 배경이 어지러워 장애물 구분 불가 | 중경 채도 -50% 필터 실적용 + 렌더 높이 55~65%로 축소, 지면 위 캐릭터 1.5배 영역 비우기. 원칙: 게임플레이 평면만 진하게 |
| 2 | 장애물 크기 제각각 | config에 OBSTACLE_RENDER 상수표(cell 54px 기준). ~~props 레인 제외~~ → **E3.8에서 콘·표지판 정식 장애물 승격으로 변경** |
| 3 | 슬라이드 스프라이트 열화 | ~~기존 slide.png 롤백~~ → **신규 누운 포즈 이미지(나노바나나 수정본)로 교체 + 렌더 수리**: 하단 지면 안착, 균일 축소 금지(높이 ~63px·폭 확대, 부피 유지) |
| 4 | 버스정류장 어색 | 캐릭터 1.6배 높이 고정·지면 안착, 안 되면 벡터 롤백 |
| 5 | 지면 단면 돌 과다 | 명도 -25%·채도 감소로 균일 톤 |

유지 확정(재작업 금지): 코인 크기·모양, 러닝 사이클, 하늘 크레인, 클리어 화면 조작 이수 연출,
E3.7 6~9 반영분(hudPanel, 매달린 자재, COIN_MAX_H, 투척물 소멸 보강), E3.6-1 점프 물리.
프롬프트는 별도 전달본 사용(채팅 기록). 검증 기준: 정지 화면에서 장애물이 0.3초 안에 식별되는가.

### E3.8. 투척 리워크 + 장애물 정합 (2026-07-16 2차 리뷰)

1) **투척 낙하 지점 방식 전환**: 직선 투척(HIGH/LOW) 폐지. 박소장이 포물선으로 머리 위를 넘겨 던지고,
   김반장 전방(속도 기준 1.5~2초 도달 거리) 지면에 낙하 마커(그림자 타원+'!')를 1.5~2초 선행 표시 후 낙하.
   낙하 파편 0.3초 후 소멸, 낙하 순간 해당 지점 피격. 난이도 램프는 낙하 간격 단축·2연속 패턴.
   슬라이드 위협은 lowbar·obs_air(매달린 자재)가 담당. 투척 대사·모션 유지.
2) **콘·표지판 정식 장애물 승격**: OBSTACLE_RENDER 편입(콘 55·표지판 75), 충돌 부여(히트박스는 렌더 -15%),
   스폰 풀 낮은 빈도 추가. 원칙: 지면 레인의 진한 오브젝트는 전부 충돌체.
3) 배경 컨테이너 더미를 중경 톤다운 필터에 포함.
4) **슬라이드 교체·렌더 수리**(신규 이미지 확보 후): 누운 포즈로 교체, 하단 지면 안착,
   높이 ~63px·폭 확대(균일 축소 금지), 슬라이드 먼지 활성, SLIDE.HITBOX_SCALE 불변.

### E3.9. 장애물 기하 정합 — 점프 클리어런스 (2026-07-16 3차 리뷰)

배경: 장애물 체감 크기 부족(원인: AI PNG 투명 여백 미트리밍) + 1단 점프로 장애물 통과 불가
(원인: 높이 70px↑ 장애물은 기본 속도에서 체공 폭이 물리적으로 부족. 2단 정점은 이미 화면 상단이라 점프 상향 불가).

1) 스프라이트 로드 시 알파 트리밍 후 OBSTACLE_RENDER 적용.
2) 장애물 3계층 재정의: 1단층(H≤60: cone 55×58, puddle 90×14, stack 60×60, sign 60×60) /
   2단층(fence 신규 50×105, prop_fence_panel 사용) / 슬라이드층(lowbar 틈 52).
3) 클리어런스 헤드리스 어설트: BASE 속도에서 체공(H+10) 수평거리 ≥ W+52+30. 상시 검증(빌드 게이트).
4) 스폰 최소 간격 ≥ 속도×0.7초(절차 스폰·route_edu 공통).
5) fence는 엔들리스 중반부터, 교육은 구간 2에 1회.

### E3.10. 일시정지 · 스프라이트 정규화 · 장애물 가시성 (2026-07-21 리뷰)

1) 캐릭터 전 프레임 실측 키 126px 통일 정규화(점프 시 커지는 버그 — v2 러닝과 구 액션 프레임 스케일 불일치).
2) 우상단 일시정지 버튼(점프 입력과 분리, visibilitychange 자동 일시정지).
   포기 = 엔들리스는 현재 기록으로 종료·결과 전달 / 교육은 로비로(미저장).
3) 장애물 대비 강화: 접지 그림자 + 실루엣 2px 다크 아웃라인 + 폭 +10%(클리어런스 게이트 내).
   웅덩이 에셋은 경고색 액센트 버전으로 재생성 예정(에셋 트랙).
확인된 개선(유지): 중경 v2 효과, 김반장 v2 러닝. 박소장 디자이너 시트 교체 예정 — 1번 정규화가 선행 조건.

### E3.11. 아이템 안내 · 결과 화면 반응형 (2026-07-21 2차)

1) 아이템 첫 등장 라벨(종류별 기기당 2회): 커피·하트 "안전모 +1" / 부스터 "5초 무적 질주!" / 자석 "5초 코인 자석". 획득 시 동일 문구 플로팅.
2) 결과·클리어 화면 반응형: 캐릭터 하단 이동(저높이 숨김), clamp 간격, 버튼 3개 상시 가시, safe-area. E4 랭킹 화면에 동일 규칙 재사용.
3) 점수 내역 분리 표기: 코인 환산 +N점 / 거리 +N점 (교육은 조작 이수 유지).

### E3.12. 아이템 스프라이트 교체 (커피·구급상자)

`assets/incoming_ai/items/item_coffee.png`(제네릭 파란 캔커피), `item_firstaid.png`(흰 구급상자+초록 십자) 반입 대기. heart는 firstaid로 개명 검토(십자가 "회복" 의미에 더 맞음, 표시 라벨은 기존대로 "안전모 +1" 유지).

```
[클로드코드 프롬프트 — E3.12]
아이템 스프라이트를 교체해줘.
1) assets/incoming_ai/items/item_coffee.png → 기존 coffee 아이템 스프라이트 교체.
2) assets/incoming_ai/items/item_firstaid.png → 기존 heart 아이템 스프라이트 교체
   (아이템 종류(heart)·효과·라벨 문구 "안전모 +1"는 그대로, 이미지만 구급상자로).
3) 두 아이템 다 다른 스프라이트 대비 크기가 튀지 않게 렌더 규격(기존 코인·아이템 크기 기준) 맞춰 축소.
sprites_manifest.json 갱신, npx tsc --noEmit 통과 후 인게임 스크린샷으로 두 아이템 크기 비교 확인.
```

### E3.13. 웅덩이 경고색 교체 + 교육 모드 안내 배너

`assets/incoming_ai/obstacles/obstacle_puddle_v2.png`(경고 테두리+콘, 지면 얼룩과 구분됨) 반입 대기.

```
[클로드코드 프롬프트 — E3.13]
1) assets/incoming_ai/obstacles/obstacle_puddle_v2.png로 기존 puddle 장애물 스프라이트 교체
   (OBSTACLE_RENDER 규격·히트박스 불변, sprites_manifest.json 갱신).
2) 안전교육(edu) 모드 첫 피격 시 1회 안내 배너: "교육 중엔 안전모가 닳지 않아요 — 본게임에선 조심!"
   (기존 구간 배너 시스템 재사용, 판당 1회만, HP 관용 로직 자체는 변경하지 말 것 — 의도된 동작).
npx tsc --noEmit 통과 후 스크린샷 확인.
```

### 아트 에셋 트랙 (병행, 코드 외)

레퍼런스 수준의 체감 격차는 배경·장애물 아트가 원인. 캐릭터는 현 수준 유지 가능.

| 대상 | 현재 | 제작 방법 |
|---|---|---|
| 배경 근경·중경 레이어(건물·자재·펜스 디테일) | 단색 실루엣 | AI 생성 또는 외주(Fiverr 배경 세트) |
| 장애물 4종(웅덩이·자재더미·lowbar 비계·낙하물) | 도형+단색 | AI 생성 후 외곽선 통일, 김반장 스프라이트와 톤 매칭 |
| 지면 타일(상단 마감 라인 포함) | 단색 밴드 | 동일 |
| 코인(안전모 형태) | 원형 | 동일 |

AI 생성 공통 프롬프트 베이스: "flat vector cartoon game asset, bold outline, chunky rounded shapes,
warm construction-site palette, style-matched to a cartoon Korean construction worker character,
transparent background, single object: [대상]". 생성물은 public/assets/sprites/에 기존 매니페스트 규격으로 반입.

---

## E4. 주간 시즌 · 일일 베스트 랭킹

목표: 기획 3장 랭킹 구조(하루 점수=일일 베스트, 주간 점수=일일 합, 월요일 리셋 4라운드)의 **클라이언트 표시 + 서버 계약**. 서버 확정 전까지 닉네임 스텁과 같은 패턴의 API 스텁으로 동작.
대상: `app/api/season/route.ts`(신규 스텁), `app/ranking/page.tsx`(주간 랭킹 뷰), `Game.tsx`(로비 D-day·내 주간 점수), `lib/api.ts`.
완료 기준(AC):
- 결과 전달 시 서버가 `{userId, nickname, score, mode, date}`를 받아 **그날 베스트만 갱신**(edu 모드 점수는 랭킹 제외).
- GET 주간 랭킹: `{round, endsAt, entries:[{rank, nickname, weekScore, days:[...]}], me:{...}}`. 주차는 이벤트 시작일 기준 월요일 리셋으로 계산.
- 랭킹 화면: "N라운드 · 종료까지 D-x" 헤더, 주간 순위(내 순위 고정 표시), 티어 배지 유지. 노선별 탭 제거.
- 로비에 "이번 주 내 점수·순위" 요약 1줄 + D-day.
- 스텁은 메모리 저장(운영 교체 전제) — 계약(요청/응답 JSON)을 파일 주석에 명시.

```
[클로드코드 프롬프트 — E4]
docs/야리끼리_한달챌린지_피벗기획_v1.md 3장대로 주간 시즌 랭킹을 구현해줘.
1) app/api/season/route.ts 스텁 생성(닉네임 스텁 app/api/nickname/route.ts와 같은 패턴, globalThis 메모리).
   POST {userId, nickname, score, mode, date}: mode가 "endless"일 때만 해당 날짜의 베스트로 갱신.
   GET ?userId=: 이번 라운드(이벤트 시작일 KST 기준 월요일 리셋, config에 EVENT_START 상수) 랭킹
   {round, endsAt, entries[{rank, nickname, weekScore}], me{rank, weekScore, todayBest}}를 반환.
   운영 서버 교체 전제 — 계약을 파일 주석에 명시해.
2) 게임 종료 시(sendResultToNative와 병행) 엔들리스 결과를 /api/season에 POST(베스트 후보 전달).
3) app/ranking/page.tsx를 주간 랭킹 뷰로 개편: "N라운드 · 종료까지 D-x" 헤더, 상위 목록 + 내 순위 고정 행,
   티어 배지 유지, 노선별 탭 제거.
4) 로비에 내 주간 점수·순위 1줄과 D-day를 표시(조회 실패 시 조용히 숨김).
5) 보상 정책 세이프 모드(법적 검토 반영, config에 REWARD_SAFE_MODE = true 플래그로):
   - 결과 화면(S4)의 "예상 포인트"(POINT_RATE × rankScore) 표시를 제거하고,
     "오늘 참여 보상 지급 예정" 같은 성적 무관 정액 안내로 교체.
   - 티켓 충전 화면(S5)에서 "포인트 교환" 버튼을 숨김(코드는 보관). 광고·초대만 노출.
   - 플래그를 끄면 기존 동작으로 복귀 가능하게(법무 결론에 따라 전환).
npx tsc --noEmit 통과, 엔들리스 2판 플레이 후 그날 베스트만 반영되는지 확인하고 요약 보여줘.
```

---

## E5. 주차 테마 팔레트 스왑

목표: 배경 아트 추가 없이 주차(라운드)별 분위기 변주. 기획 5.1.
대상: `Background.ts`, `config.ts`(주차→테마 매핑).
완료 기준(AC):
- 엔들리스 배경에 라운드별 필터 프리셋: 1R 낮(원본) / 2R 노을(hue·warm) / 3R 야간(어둡게+파랑, 필요 시 글로우 1장) / 4R 터널·심야(대비 강조).
- 캔버스 필터(ctx.filter: hue-rotate/brightness/saturate) 또는 오프스크린 사전 처리로 프레임 드랍 없이.
- 라운드는 E4의 round 값을 사용(스텁이면 EVENT_START 기준 계산).

```
[클로드코드 프롬프트 — E5]
배경 팔레트 스왑을 구현해줘 (docs/야리끼리_한달챌린지_피벗기획_v1.md 5.1).
1) config.ts에 라운드(1~4)→테마 프리셋(hue-rotate/brightness/saturate 값) 매핑을 추가.
2) Background.ts에서 엔들리스일 때 현재 라운드의 프리셋을 적용. ctx.filter를 매 프레임 걸면 느릴 수 있으니
   레이어 이미지를 오프스크린 캔버스에 한 번 필터 적용해 캐시하는 방식으로.
3) 저사양에서 프레임 확인(delta-time 유지).
npx tsc --noEmit 통과, 라운드 값을 바꿔가며 4테마 스크린샷 확인 후 요약 보여줘.
```

---

## E6. QA · 밸런싱 · 폴리시

목표: 이벤트 오픈 게이트. v2의 WP9 항목에 피벗 특화 항목을 더한다.
체크리스트:
- **양손 조작**: iOS/AOS 웹뷰에서 좌 홀드 + 우 연타 시 핀치줌·뒤로가기 제스처 오인 여부, pointercancel 처리.
- 엔들리스 밸런스: 평균 생존 60~120초가 되도록 SPEED/SPAWN/CHASE 튜닝. 일일 베스트 구조상 "한 판 더"가 나오는 길이가 목표.
- 안전교육 이수율: 내부 테스트에서 첫 판 완주율 90% 이상. 미달 시 관용 강화 또는 길이 단축.
- 닉네임: 중복·금칙어 케이스, 서버 스텁 재시작 시 로컬 캐시로 복원되는지.
- 점수 어뷰징: playDuration 대비 score 상한 검증(서버 계약에 포함), edu 점수 랭킹 제외 확인.
- 로깅: 플레이 시작/종료(mode 포함), 티켓 소진, 교육 이수, 랭킹 조회 이벤트.

```
[클로드코드 프롬프트 — E6]
이벤트 오픈 전 QA·밸런싱 패스를 진행해줘.
1) 양손 조작(좌 홀드+우 연타) 웹뷰 제스처 충돌·pointercancel 누락을 점검하고 필요한 방어 코드(touch-action, preventDefault)를 보강해.
2) 엔들리스 평균 생존 60~120초 목표로 SPEED/SPAWN/CHASE/PROJECTILE 수치 조정안을 표로 제시하고 적용해.
3) 기본 이벤트 로깅(플레이 시작/종료+mode, 티켓 소진, 교육 이수, 랭킹 조회)을 추가해.
4) /api/season POST에 playDuration 대비 score 상한 검증을 넣어(초당 최대 획득 점수 기준).
결과 요약과 config 변경 전후 표를 보여줘.
```

---

## 서버·앱 팀 의존 (웹게임 밖 작업 — 별도 전달)

1. 닉네임·시즌 API 운영 이관: `app/api/nickname/route.ts`, `app/api/season/route.ts` 주석의 계약대로 DB 기반 구현.
2. 진행 저장 서버 이관: 안전교육 이수·티어를 userId 기준 서버 저장(현재 localStorage).
3. 배너·팝업: 상태 기반 문구(내 순위·티켓 소멸), 라운드 시작일 팝업, UTM. 추월 푸시.
4. 보상 지급: 월간 top3 상품 + 주간 포인트 + 참여 추첨 — 지급·본인 확인은 가다 계정 권위.

## 참고

- 각 단계 완료 후 `npx tsc --noEmit` 통과와 실제 플레이 확인을 기본 게이트로 둔다(`npm run build`는 최종 확인 시).
- route2~5 JSON·별점 코드·상점 관련 코드는 **삭제 금지, 미사용 보관**(이벤트 중 보너스 미션 등 재활용 여지).
- 재화(티켓/포인트) 지급·차감은 네이티브/서버 권위. 웹은 표시·요청·결과 전달만.
